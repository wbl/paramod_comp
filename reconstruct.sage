def reconstruct_mat(M, bound, rows, rowlist):
    ''' Determine an A integral with positive entries bounded by bound commuting with M with rows given by rows for rows in rowlist that is integral if a unique such exists '''
    # Sanity check
    # Assemble problem
    # Pick modulus
    # Check rank
    # Solve
    pass
